@model MyBooks.Models.User

@{
    ViewData["Title"] = "Details";
}


<div class="container mt-4">
    <h1 class="mb-3">Detaljer</h1>

    <div class="card shadow-sm p-4 bg-light">
        <dl class="row">
            <dt class="col-sm-3 fw-bold text-primary">
                @Html.DisplayNameFor(model => model.User_Name)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.User_Name)
            </dd>

            <dt class="col-sm-3 fw-bold text-primary">
                @Html.DisplayNameFor(model => model.Email)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Email)
            </dd>
        </dl>
    </div>

    <div class="mt-3 d-flex gap-2">
        <a asp-action="Edit" asp-route-id="@Model?.UserID" class="btn btn-primary">Redigera</a>
        <a asp-action="Index" class="btn btn-outline-secondary">Tillbaka till kontolista</a>
    </div>
</div>
