@model MyBooks.Models.User

@{
    ViewData["Title"] = "Delete";
}


<div class="container mt-4">
    <h1 class="text-danger mb-3">Ta bort konto</h1>

    <div class="alert alert-warning">
        <h3 class="mb-0">Är du säker att du vill ta bort detta konto?</h3>
    </div>

    <div class="card shadow-sm p-4 bg-light">
        <dl class="row">
            <dt class="col-sm-3 fw-bold text-primary">
                @Html.DisplayNameFor(model => model.User_Name)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.User_Name)
            </dd>

            <dt class="col-sm-3 fw-bold text-primary">
                @Html.DisplayNameFor(model => model.Email)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Email)
            </dd>
        </dl>
    </div>

    <form asp-action="Delete" class="mt-3">
        <input type="hidden" asp-for="UserID" />
        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-danger">Ta bort</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Tillbaka till listan</a>
        </div>
    </form>
</div>
